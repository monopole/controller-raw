apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: reboot-controller
spec:
  replicas: 1
  template:
    spec:
      containers:
      - name: reboot-controller
        image: monopole/demo-reboot-controller:cca50bdb5aaa3563dd58780df131bb893334e915
        command:
        - /reboot-controller
